# Lockfile specification for the operator and operand containers
#
# podman run --rm -v "$PWD:$PWD:z" -w "$PWD"  registry.redhat.io/ubi9/ubi  cp -r /etc/yum.repos.d/. .
# ~/.local/bin/rpm-lockfile-prototype -f Dockerfile.operator rpms.in.yaml --outfile rpms.lock.yaml
contentOrigin:
  repofiles:
    - ubi9.repo
packages:
  - git
  - golang
  - patch
arches:
  - x86_64
  - aarch64
  - s390x
  - ppc64le
context:
  containerfile:
    file: Dockerfile.operator
